GAIT 1.8.0-alpha5 validation

Source contracts and behavioral tests: PASS against this checkout.
HEMTT 1.21.0: PASS; config 1.8.0.5, 14 SQF files, one PBO.
Final post-carry-correction build completed successfully.
Package verification follows in the final archive step.
Requires Arma 3 2.18 for array switchMove/getUnitMovesInfo.

Preservation + mutation checks: PASS. All 25 historical RC4 block hashes remain:
19 intact blocks; 6 changed blocks with 14 exact reviewed reversals.
Original presets and slope pace model are byte-identical. All original CBA
ranges/defaults remain; four release descriptions and the inactive sustain
label now explain the bounded curve. Original tier relief and launch duration
are restored. Mutation checks reject retuning those restored values, removing
the forward-input gate, extending the endpoint, filtering the direct curve,
changing sprint intent, and other protected brace/reserve/ramp behavior.

Graph: PASS; all 4 contract tests. 72 states and 8 action maps unchanged.

All 14 actual-helper-source SQF-VM suites: PASS, zero diagnostics.
- gear_inertia: original inclusive relief tiers and custom thresholds; original
  launch duration; modest acceleration (scale 0.90-1.00); default release under
  0.5 seconds; configured release 0.20-0.65 seconds; no hold; direct monotonic
  release curve across ten loads and five pace pairs; exact/late endpoints at
  five tick rates; no-W/stop/re-tap/deadline cancellation; forward diagonals;
  no residual animation tail; uphill brake priority.
- locomotion_coast: live input priority; W+A/D response; released-W cancellation;
  stale coast lease rejection; pending entry cleanup; actual tick dispatcher
  cannot replace a deferred exit; forward release observed during cleanup.
- locomotion_handoff: aim/phase arguments; brace promotion; token cancellation;
  promotion-timeout cleanup; held sprint can recover to native sprint intent;
  input-history transitions. Engine restriction queries remain at live edges.
- release_and_blend: argument safety; recognized custom/native blends; strict
  lowered-rifle walk/tactical _ver2 handling; unknown suffix rejection.
- uphill_brake: signed grade, release edges, scaling, re-tap, bounded braking.
- uphill_brake_handoff: prearm, lease expiry and protected actions.
- brace_momentum: actual movement history, all-trigger protection, stop rearm.
- downhill_pace: angle/load ordering, bounds, time consistency and recovery.
- locomotion_state_machine: entry/release, escape, direction and ownership.
- input_and_ramp: immediate input samples and time-based coefficients.
- slope_direction: direction selections, reversal and release.
- slope_state_selection: real sprint and appropriate lateral/rear clips.
- locomotion_pace: fallback/physical-unit interface and invalid calibration.
- ace_status_bridge: event order, medical masks and physiology ownership.

Reproduce with tools/run_regressions.py and caller-supplied SQF-VM.
The runner combines current helper sources, not cached function copies, and
records commands, SHA256 hashes, PASS evidence and completion markers.

Limitations: SQF-VM does not simulate Arma root motion, animation blending,
camera, collision or network replication. The getUnitMovesInfo dummy permits
parsing only and is not executed by these tests. Actual smoothness, stopping
distance, steep-slope speed and multiplayer behavior require game checks.
Windows deployment remains a target-platform check.
