GAIT 1.8.0-alpha11 validation — 2026-09-19

HEMTT 1.21.0 build: PASS. Version 1.8.0.11; 18 compiled SQF scripts; 2 PBOs.
Existing style advisories remain. Wiki refresh fell back to bundled metadata.
No raw model/texture binarization was required. Arma runtime unavailable.

All 22 actual-helper SQF-VM suites: PASS, zero diagnostics.
Source hashes match final code; portable evidence: VALIDATION_RESULTS.json.
New suites exercise stable passive observations, invalid/context-changing
samples, grade/age-scoped reference lookup, unequal clip speeds, measured
release endpoints, 101 blend weights, forward/reverse handoffs, retap seeds,
expired/context-cancelled bridges and unknown-animation fallback.
Engine context and clock boundaries are mocked; Arma physics is not simulated.

Settings audit: 6 tests PASS. 122 unique live registrations, valid defaults,
preset references/ranges, five retired controls absent, effective range/mode
corrections and unconditional lifecycle cleanup. The source audit confirms
readers and connections; it does not click every CBA option in a running game.
Pace integration: 4 tests PASS. Loader, observer, endpoint, same-writer and
bounded handoff/retap links; calibration contains no direct motion writes.
Animation graph: 7 tests PASS. Heartbeat configuration: 3 tests PASS.
Prefix/source payload: 4 tests PASS. Both PBOs have correct disjoint prefixes,
valid SHA1 trailers and embedded runtime scripts identical to final source.

Preservation and mutation self-tests: PASS. All 25 historical RC4 block hashes
and three file reference hashes remain unchanged. Fifteen exact alpha11
reversal groups precede the 44 historical authorized reversals. Defaults and
unrelated gameplay stay protected except the documented settings cleanup.
Alpha10 gear/brace, uphill/downhill, pace-model and fatigue-visual helpers
remain byte-identical. No aim/native-fatigue/recoil writes were introduced.

PowerShell 7.4.7 verification on Linux: PASS for both the fresh build and
bundled standalone mod. All seven compiled heartbeat gains match 0.224403691
(20% of original db+1 amplitude). ZIP CRC and SHA256 manifest are verified
on creation.

Reproduce from project root:
  hemtt build
  python tests/feature_preservation.py --self-test
  python tests/settings_audit.py
  python tests/pace_calibration_integration.py
  python tests/foundation_graph.py
  python tests/heartbeat_audio.py
  python tests/pbo_prefix_layout.py
  python tools/run_regressions.py --sqfvm /path/to/sqfvm --output /tmp/gait-tests
  powershell.exe -NoProfile -ExecutionPolicy Bypass -File VERIFY_READY.ps1

Limits: passive jog references require stable eligible travel in the current
weapon/surface/grade context. Missing references retain alpha10 release.
The two-clip blend model, actual cross-clip velocity, animation appearance,
stopping distance, perceived loudness and visual effects require in-game
acceptance. A shorter blend does not guarantee halved travel distance.
Windows deployment/junction behavior remains a target-platform check.
