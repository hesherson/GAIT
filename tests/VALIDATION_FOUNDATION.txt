GAIT 1.8.0-alpha6 validation

HEMTT 1.21.0: PASS; config 1.8.0.6, 14 compiled SQF files, one PBO.
Build retains nonblocking style advisories. No Arma runtime was available.
Final archive validation checks prefix gait, config.bin, matching source bytes
for all fourteen embedded scripts, source manifest and ZIP integrity.

Source preservation and mutation checks: PASS.
All 25 historical RC4 block hashes retained: 17 intact blocks and 8 blocks
with 16 exact authorized reversals. Original presets and slope pace model
remain byte-identical. Original settings ranges/defaults remain; descriptions
explain alpha6 brace relief and the shorter forward ramp. Four additional
source assertions protect native stamina acquisition order and trip/player
change/reset cleanup. Removal and tuning mutations are rejected.

Graph: PASS, 5 contracts. 36 states and 4 action maps. Every forward custom
movement state inherits a real Meva sprint clip. No custom walking-brace
states or switchMove calls remain. Graph generation matches shipped config.

All 15 actual-helper-source SQF-VM suites: PASS, zero diagnostics.
- native_stamina_ownership: acquisition/release, original enabled flag, player
  replacement, disabled settings/context, pre-disabled flag, external enable,
  locality loss and unchanged ACE medical masks. Engine flag access is mocked.
- brace_momentum: 6 m/s sprint to settled 4 m/s W-only jog rearms brace across
  three pace coefficients; quick moving retap avoids another brace; ongoing
  steep sprint and actual-stop behavior remain protected. The new settled-jog
  case fails against the alpha5 helper, demonstrating the old regression.
- gear_inertia: original relief settings and duration, bounded acceleration
  scale 1.00-1.06, heavy baseline 1.00, default finite release 0.268-0.342 s,
  configured bounds 0.15-0.45 s, monotonic endpoints across ten loads/five
  pace pairs/five tick rates, no-W/stop/retap cancellation and uphill priority.
- locomotion_coast: immediate raw release, safe fresh-press exit reversal,
  no held-key restarting, deferred unsafe cleanup and W-release history.
- locomotion_handoff: exact entry/exit blend recognition, interrupted-blend
  source grace, numeric brace cancellation and no walking-stage promotion.
- release_and_blend: strict native/custom movement endpoints and supported
  lowered-rifle variants; unrelated suffixes and unsafe transitions rejected.
- uphill_brake_handoff: brake remains numerical, cannot hold sprint clip.
- uphill_brake: signed grade, release edges, scaling, retap and bounded brake.
- downhill_pace: angle/load ordering, bounds, time consistency and recovery.
- locomotion_state_machine: entry/release, escape, direction and ownership.
- input_and_ramp: immediate input samples and time-based coefficients.
- slope_direction: direction selection, reversal and release.
- slope_state_selection: genuine sprint and appropriate lateral/rear clips.
- locomotion_pace: fallback/physical-unit interface and invalid calibration.
- ace_status_bridge: event order, medical masks and physiology ownership.

Reproduce with tools/run_regressions.py and caller-supplied SQF-VM.
The runner combines current helper sources and records commands, source
SHA256 hashes, PASS evidence and completion markers. Tests do not execute
live animation commands or simulate Arma's native stamina system.

Limits: these checks cannot establish heavy-backpack sprint permission,
root motion, animation/camera blending, collision or multiplayer in Arma.
Check the focused acceptance cases in README_TEST_BUILD.md in a fresh mission.
The read-only foundation capture records native stamina ownership, permission
flags, ACE masks, backpack/load and exact entry/exit animation observations.
Windows deployment remains a target-platform check.
