GAIT 1.8.0-alpha7 validation

HEMTT 1.21.0: PASS; config 1.8.0.7, 16 compiled SQF files, 2 PBOs.
Nonblocking style advisories remain. No Arma runtime was available.
Both PBO prefixes and all 16 embedded scripts match source. The compiled
heartbeat config contains all 7 original sample paths, unchanged pitch and
half-gain values of 0.561009227. ZIP integrity and source manifest pass.

All 17 actual-helper SQF-VM suites: PASS, zero diagnostics.
Recorded source hashes match the final checkout. All 15 alpha6 suites remain.
New coverage:
- fatigue_visuals: 1.30-second pulse, 14% opacity cap, 5-12-second clear gaps,
  smooth rise/fall, late-frame completion, recovery, cooldown retention through
  brief zero/rebound input, owned-handle cleanup, 0.75-second stale request
  expiry, setting/context/player cleanup, and ACE visual bridge dispatch.
- ace_fatigue_visual_bridge: acquisition and restoration of only the published
  ACE Advanced Fatigue blackout handle, original enabled flag preservation,
  pre-disabled handles, external enable, ACE disable, player replacement,
  handle replacement/removal, and avoiding commands to stale numeric IDs.
Engine post-processing access is mocked; tests do not render pixels.
The gear suite checks heavy acceleration 1.02, lighter maximum 1.06, and
unchanged brace duration, release curve, endpoints and input cancellation.

Graph contracts: 5/5 PASS; 36 states and 4 maps unchanged from alpha6.
Heartbeat contracts: 3/3 PASS; all 7 variants use half the original gain,
unchanged samples/pitch, and an optional ACE Medical Feedback dependency.

Preservation and mutation checks: PASS. All 25 historical RC4 block hashes
remain: 14 intact blocks and 11 blocks with 26 exact authorized reversals.
The original slope model remains byte-identical. Exact preset reversals
restore 8 retired sway/native-fatigue entries; all other preset bytes remain
protected. Settings permit only reviewed changes.
Four stamina and nine visual integration assertions protect call ordering,
per-tick updates, watchdog installation, context guards and cleanup. Mutations
that add aim, native-fatigue or recoil writers fail. Existing brace, reserve,
ramp, momentum and uphill regression mutations continue to fail.

Package comparison confirms that the alpha6 locomotion controller, input
helpers, brace history, uphill/downhill helpers, slope model, generated graph
and existing PAA/OGG assets are byte-identical. The gear adjustment is limited
to a 2% faster heavy acceleration rate, blended into lighter-load ordering.

Reproduce from the project root:
  python tests/feature_preservation.py --self-test
  python tests/foundation_graph.py
  python tests/heartbeat_audio.py
  python tools/run_regressions.py --sqfvm /path/to/sqfvm --output /tmp/gait-tests

Limitations: automated checks cannot render Arma post processing, simulate
engine aim/animation behavior or measure perceived loudness. Heavy gear feel,
ADS sway, vignette appearance and coexistence with medical effects require
in-game verification. Windows deployment remains a target-platform check.
